/*function render() {
    if (store.view === 'start') {
      $('.intro').show();
      $('.quiz').hide();
      $('.status').hide();
    } else if (store.view === 'quiz') {
      $('.intro').hide();
      $('.quiz').show();
      $('.status').show();
    }
  } 
  */